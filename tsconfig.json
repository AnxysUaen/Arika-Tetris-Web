{
    "compilerOptions": {
      "moduleResolution": "node",
      "resolveJsonModule": true, // 允许导入JSON文件
      "target": "ES2024",        // 编译为现代浏览器支持的ES2024语法
      "module": "ES6",           // 使用ES6模块化标准
      "outDir": "./dist",        // 输出目录（存放编译后的JS文件）
      "rootDir": "./src",        // 源代码目录（存放.ts文件）
      "strict": true,            // 启用严格类型检查
      "esModuleInterop": true,   // 解决模块兼容性问题
      "baseUrl": "./",           // 基础目录
      "paths": {
        "@tetris/*": ["src/tetris/*"],
        "@utils/*": ["src/utils/*"],
        "@lib/*": ["src/lib/*"],
      }
    }
  }